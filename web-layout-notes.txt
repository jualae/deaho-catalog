Web layout issues identified:
1. Cards are 2 columns but each card is very wide (~480px each) - total ~960px content area
2. Thumbnail images inside cards overflow or are misaligned - images don't fit properly in the card
3. The card has a thumbContainer that shows 4 thumbnail images in a row, but on web they appear too large
4. The info section (name bar, description, footer) looks okay
5. Main issue: the category-card component was designed for mobile width (~180px per card) but now each card is ~480px wide on web
6. Need to either: a) limit card max-width, or b) make thumbnails responsive to card width
7. The gap between CSS approach and RN approach is causing issues - need a cleaner solution
8. Best approach: use CSS grid in global.css for web, keep card component simple and let it fill its container
