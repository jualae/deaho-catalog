Web layout check round 3:
- Scrollbar is now visible on the right side (confirmed in screenshot)
- 2-column grid working at current viewport width (~1000px)
- Cards display with proper thumbnails and info
- Need to verify 3-col and 4-col at wider viewports
- The breakpoints are: >=1200px = 4cols, >=900px = 3cols, <900px = 2cols
- Current viewport appears to be around 1000px so should show 3 cols but showing 2
- Issue: the windowWidth might be the full browser width, but the gap calculation uses it
- Actually looking at the screenshot, the content area is narrower due to the previous max-width CSS being removed but the content still appears constrained
- The cards are 2 per row which is correct for the current effective width
